package javaFastTrack2.day06;

public abstract class OOP_Practice extends Object{
	// what is the difference between the abstract class and interface.
	//*** we do not want anybody to create any instance from that class ***
	// we have abstract methods, but we do not implement them, we have to implement them in the sub class
	// but we do not have to implement it if the sub class is also an abstract class
	
	abstract void method();
	
	// polymorphism
	// showing a type with its super type is called polmorphism. and we use polym. when we work with the data structures. 
	
}

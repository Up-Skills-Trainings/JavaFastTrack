package javaFastTrack1.day04;

public class Curriculum {
	/*
	 * Course Curriculum
	 * 
	 * 1. If/else statements 2. Basic math operations as a starting point of
	 * algorithms 3. String (substring, replace, charAt etc)
	 * 
	 * swapped >> arrays and loops 4. Loops (for, for each (after the arrays)) 5.
	 * Custom method (void & return both) 6. Arrays (single dimensional ONLY)
	 * 
	 * 7. Class & object (static & instance, constructor) 8. Encapsulation 9.
	 * Inheritance 10. Abstraction (in general ONLY) 11. Polymorphism 12. Collection
	 * (List & Set)/Not detailed.
	 * 
	 * ==========================================================
	 * 
	 * The purpose: is to pass technical interview
	 * 
	 * We will: keep it simple, as much as we can.
	 * 
	 * We will: only focus on some basics that will help you to pass technical
	 * interview like static, basic algorithms, overriding, overloading,
	 * abstraction.
	 * 
	 * Most of our time: we will try to solve questions with loops and arrays, and
	 * we will practice some OOP concepts.
	 * 
	 * 
	 * 
	 * Day 4
	 * 
	 * Array is an hidden object in java Fixed size "new" keyword If new >> heap >>
	 * has first value If no new >> stack >> no first value Index out of bond
	 * exception
	 * 
	 * I need dynamic memory, and we use arrays for that.
	 ** 
	 * CALL BY VALUE, CALL BY REFERENCE
	 * 
	 * ==========================================================
	 * 
	 * int[] reference;
	 * 
	 * (reference[5] = 10;)
	 * 
	 * reference = int[10];
	 * 
	 * ==========================================================
	 * 
	 * there is a reference, there is a heap memory, reference is in the stack
	 * memory
	 * 
	 * ==========================================================
	 * 
	 * primitive types no initial value stack >> no first value if we use new >>
	 * heap >> first value yes
	 * 
	 * ==========================================================
	 * 
	 * int[] arr = new arr[20]; int[20]; Index Out of Bond
	 * 
	 * ==========================================================
	 * 
	 * we do not use the arrays, we use data structures
	 * 
	 * ==========================================================
	 * 
	 * garbage collector,
	 * 
	 * ==========================================================
	 * 
	 * display the array, we can use Math.random() * 100 >> show two digit numbers
	 * 
	 * ==========================================================
	 * 
	 * syso(array) >> returns hashcode
	 * 
	 * ==========================================================
	 * 
	 * char[] ch = {'D', 'a'}; syso(ch) >> will not return the hashcode >> is String
	 * ==========================================================
	 * 
	 * sum the array
	 * 
	 * ==========================================================
	 * 
	 * find the largest element show the sorting algortihm from link
	 * ==========================================================
	 * 
	 * finding the smallest index of largest element
	 * 
	 * ==========================================================
	 * 
	 * largest element index = -1;
	 * 
	 * ==========================================================
	 * 
	 * random shuffling
	 * 
	 * ==========================================================
	 * 
	 * shifting elements array[i + 1] >> do not get IOOBE
	 * 
	 * ==========================================================
	 * 
	 * foreach loops
	 * 
	 * ==========================================================
	 * 
	 * copying arrays with example print before copy print after copy
	 * 
	 * 
	 * how to make actual copy of elements
	 * ==========================================================
	 * 
	 * call by value, call by reference
	 * 
	 * ==========================================================
	 * 
	 * reverse the string str.toCharArray();
	 * ==========================================================
	 * 
	 * search algortihm, linear search
	 * 
	 * O(n) when the data is high, not affective
	 * ==========================================================
	 * 
	 * binary search, binary search algortihm must be sorted list. effective than
	 * linear
	 * 
	 * ==========================================================
	 * 
	 * in the given array and a target number, find the numbers and indexes that
	 * gives the target number
	 * 
	 * ==========================================================
	 * 
	 * sorting the array without Arrays.sort()
	 */
	
}
